# Dockerfiles

This includes dockerfiles to build NOCFoundry release and development container images for go dev environment.

* Dockerfile: NOCFoundry Release Image
* Dockerfile-dev: Development container image for VS Code Remote-Containers and GitHub Codespaces.

